const int l = '4' + '6'; 
const int lk = 'j'; 
int jjjj;
REAL jjjk;
char jjjl;
bool jjjp;
bool jjje[5][13];

PROGRAM ops ()
{
    /* Constant expression evaluations */

    const char kk = 54;    
    const bool jk = ('4' + '6') == 'j' ;
    const bool jj = l == lk;
    const bool boo = true or false;
    const bool boon = true and true;
    const bool asdasd = !false;
    const bool asdasd2 = !true or true;
    const bool g = boon || boo;

    // lookups
    bool ndk = jj or boo and boon ; // true
    ndk = ndk; // true
    jjjj = jjjj +1;
    jjjj = jjjj ;

    char n = 'a';                   // 97
    char p = -'a';                  // 97
    char pp = -(-'a');              // 97
    char ppp = -(-(-'a'));          // 97
    char pppp = -(-(-(-'a')));      // 97
    
    bool a = !false;                // true
    bool pk = not not true;         // true
    bool pkk = not ! not true;      // false
    bool b = not (false and true or (false or true) and false or true); // false

    // signed binops and casts
    REAL k = -3 + 2;                // -1
    REAL k0 = 3 - (-2);             // 5
    REAL ka = -3 * 2;               // -6
    REAL kb = 4 * 3 / -2 ;          // -6
    REAL kc = 44 + (-2);            // 42
    REAL d = 13.0 + 1 * 'a' / 2.0 ; // 61.5

    bool ddk = -2 < 15;             // true
    bool ddj = -152 < -151;         // true
    bool ddl = -152 <= -152;        // true
    bool ddr = 152 > -152;          // true
    bool ddp = -23.1 < 11.5432;     // true
    bool ddq = 1 == 1;              // true
    bool ddo = -152 >= -152;        // true
    bool ddz = -152 > -152;         // false
    bool ddx = -152 != -152;        // false

    // Boolean operations with lookups
    
    ddx = ddj and ddr and ddk and ddl or ddz; // true
    
    char c = 's';
    char s[10];
  //  s  = "stringy";
}
