const int b = 5 ;
const REAL c = b;
const REAL d = b + 1, e = c;
const REAL p = d;

PROGRAM array_assignments ()
{
    int y[15], zoo = 125, z[12], opop = 2;
    int zp = b;
    zp = zp;
    char yl[15 % 4];        // 0 x i8
    int k[-(1-2)];          // 1 x i32
    int l[1 + 3 * 2 - 6];   // 1 x i32
    int a[5][2+1];          // 5 x [3 x i32]
    char r['a'];            // 97 x i8
    int d[b*2];             // 10 x i32
    int B[15];
    int A[15];
    int C[15];

    WRITELN(A[0]);
    WRITELN(A[15]);
    WRITELN(A[-1]);
}
