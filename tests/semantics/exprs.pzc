const int n = 10;

PROGRAM exprs ()
{
    // var_inits
    int z[10];
    int y[10][n], kk[n];
    int y2[10][10];
    int l = n;
    REAL k = 15;
    char c = 5; 
    bool d = false || true;
    int i = 's'; 
    z = y2[10];
    
    i = (3 + 4) * 2 + 5 / 5;  // 15
    d = d == d;
    
}
