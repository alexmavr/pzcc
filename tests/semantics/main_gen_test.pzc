const int n = 10;

FUNC int tocall(int hi) {
    return 1;
}

FUNC int tocall2(int my_name2) {
	return 32;
}

PROGRAM exprs ()
{
    // var_inits
    int z[10];
    char zoo[10];
    int y[10][n], kk[n];
    int y2[10][10];
    int l = n;
    REAL k = 15;
    char c = 5; 
    bool d = false || true;
    int i = 's'; 
    // z = y2[9];

    zoo = "012345678";
    WRITELN(zoo);
    
    i = (3 + 4) * 2 + 5 / 5;  // 15
    d = (d == d) and true == false or true;  // true
    i = tocall(3);
    
}
